name: iOS Build (No Codesign)

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  ios-build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter pub get
        run: flutter pub get

      - name: Ensure iOS scaffolding exists
        shell: bash
        run: |
          if [ ! -f ios/Podfile ]; then
            flutter create --platforms=ios .
          fi

      - name: Build iOS app (unsigned)
        run: flutter build ios --debug --no-codesign

      - name: Package unsigned IPA
        shell: bash
        run: |
          APP_PATH="build/ios/iphoneos/Runner.app"
          IPA_DIR="build/ios/iphoneos"
          PAYLOAD_DIR="$IPA_DIR/Payload"
          IPA_PATH="$IPA_DIR/qrtags.ipa"

          rm -rf "$PAYLOAD_DIR" "$IPA_PATH"
          mkdir -p "$PAYLOAD_DIR"
          cp -R "$APP_PATH" "$PAYLOAD_DIR/"

          cd "$IPA_DIR"
          zip -r -q "qrtags.ipa" "Payload"
          rm -rf "Payload"

      - name: Upload unsigned IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-unsigned-ipa
          path: build/ios/iphoneos/qrtags.ipa

      - name: Upload Runner.app artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-runner-app
          path: build/ios/iphoneos/Runner.app
